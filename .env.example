# Sensitive, deployment-specific identities. Copied to
# ~/.pilotage-agent/.env by scripts/install.sh and restricted to its owner.

# Phone numbers in digits, comma separated, country code included and no plus
# sign. Empty means the agent answers nobody.
PILOTAGE_ALLOWED_SENDERS=

# Default WhatsApp chat for unattended cron output (phone/JID or group JID).
WHATSAPP_HOME_CHANNEL=

# Telegram token from BotFather. Leave blank while Telegram is disabled.
TELEGRAM_BOT_TOKEN=

# Numeric Telegram user IDs, comma separated. Empty means nobody.
TELEGRAM_ALLOWED_USERS=

# Default Telegram chat and optional topic for unattended cron output.
TELEGRAM_HOME_CHANNEL=
TELEGRAM_HOME_CHANNEL_THREAD_ID=

# Public HTTPS endpoint for webhook delivery. Blank falls back to long polling.
TELEGRAM_WEBHOOK_URL=
# Required when TELEGRAM_WEBHOOK_URL is set; use a long random value.
TELEGRAM_WEBHOOK_SECRET=
# TELEGRAM_WEBHOOK_HOST=
# TELEGRAM_WEBHOOK_PORT=8443

# Optional OpenAI API key for automatic voice-message transcription. ChatGPT
# subscription authentication does not authorize the audio transcription API.
VOICE_TOOLS_OPENAI_KEY=

# Full-page web extraction. Use either a cloud key or a self-hosted URL.
FIRECRAWL_API_KEY=
FIRECRAWL_API_URL=

# SQL Server reporting connection used by the readiness check and skills.
MSSQL_HOST=
MSSQL_USER=
MSSQL_PASSWORD=
MSSQL_DB=
MSSQL_ENCRYPT=true
MSSQL_TRUST_CERT=false
# SQLCMD_BIN=/opt/mssql-tools18/bin/sqlcmd
